# Root environment variables (copy to .env and customize)
NODE_ENV=development
WEB_PORT=3000
API_PORT=4000

# Database / Cache (update for your environment)
DATABASE_URL=postgresql://appuser:password@localhost:5432/career_platform
SHADOW_DATABASE_URL=postgresql://appuser:password@localhost:5432/career_platform_shadow
REDIS_URL=redis://localhost:6379

# Security / Auth
JWT_SECRET=replace_me_with_long_random_value
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me_local_only
JWT_EXPIRES_IN=1h
NEXT_PUBLIC_API_URL=http://localhost:4000/graphql
