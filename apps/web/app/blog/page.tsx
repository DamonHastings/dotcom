export default function BlogPage() {
  return (
    <section className="space-y-6">
      <h1 className="text-3xl font-bold tracking-tight">Blog</h1>
      <p className="text-gray-600 max-w-prose">Posts will appear here once published content is added via the admin workflow.</p>
      <p className="text-xs text-gray-400">Listing query pending implementation.</p>
    </section>
  );
}
